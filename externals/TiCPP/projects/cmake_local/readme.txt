NOTE

TiCPP is not build as stand-alone library, but its files are sources into the MasterSim library directly. This
is to avoid a name-clash with installed tinyxml libraries, since we have modified the library ourselves and tinyxml
is no longer actively developed.

